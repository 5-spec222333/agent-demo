# 项目级协作说明

## 项目目标

本项目用于梳理并演示“Agent 驱动的广告素材生产平台”。先完成 HTML 产品全景大图，再基于已经确认的产品逻辑开发可交互 Demo。

项目的产品定义是：

> 面向电商、零售和用户增长运营人员，以 Agent 为交互入口，以结构化配置为生产中间层，以商品库、品牌资产库和知识库为资产底座，以多模型与工程合成为生产能力，以审核、素材库、投放和数据回流为闭环的广告素材生产平台。

目标不是简单生成图片或视频，而是把模糊的运营意图稳定转化为可确认、可生成、可审核、可入库、可投放、可追溯和可复盘的营销素材。

## 用户与协作方式

- 用户是刚入门的产品经理。先给结论，再解释原因、取舍和上下游影响。
- 默认使用简体中文；英文缩写首次出现时附中文解释。
- 使用“夏季女装生成 10 条促销视频”等具体案例解释抽象流程。
- 区分原始资料中的事实、产品推断和为原型补充的假设。
- 只有会显著改变方向的问题才阻塞询问；其他缺失信息采用保守假设并明确标注。
- 每次修改说明改了什么、为什么改、影响哪些流程或状态。

## 不可破坏的业务主链路

所有设计必须保留以下顺序：

> 运营意图 → Agent 理解 → 歧义识别与缺失信息追问 → 需求确认 → 选品与承接确认 → 创意方案 → 图片方案/视频分镜 → 预览确认 → 创建任务 → 生产队列 → 模型生成与工程合成 → 审核队列 → 素材入库 → 投放 → 数据回流 → 效果分析 → 策略沉淀

不得将产品简化为“输入 Prompt → 调用模型 → 得到成片”。

## 核心产品原则

1. Agent 必须体现需求理解、歧义识别、追问、推荐、解释、分阶段确认和任务推进，不能只是聊天皮肤。
2. 选品与承接对象必须在创意和批量生产之前确认。
3. 图片与视频使用不同生产链路；视频必须体现分镜、组件、模型片段、TTS/数字人和工程合成。
4. Schema、状态机和可追溯是基础能力，不是后补功能。
5. 生产失败支持局部重试、降级和人工接管；审核驳回应返回具体环节。
6. 每份素材都关联原始需求、商品与品牌资产、创意版本、Prompt、模型版本、合成参数、审核记录和投放结果。
7. 数据回流首期以记录、比较和观察为主，不虚构未经验证的自动归因与自主优化能力。
8. 运营只感知需要自己判断的业务决策；系统承担 Prompt 拼装、模型路由、资产调用、调度、合成、重试和数据关联。

## 核心业务域

- Agent 交互与需求确认；
- 结构化需求与任务配置；
- 选品与商品 List；
- 图片创意与生成链路；
- 视频创意、分镜与合成链路；
- 生产队列；
- 审核队列；
- 素材库；
- 商品库、品牌资产库、频道规则库和创意知识库；
- 投放与数据回流；
- 任务、审核、素材和数据看板；
- 账号、角色和权限。

## 核心状态机

- 需求：`草稿 → 解析中 → 待补充 → 待确认 → 已确认 → 已创建任务`
- 生产：`待排队 → 排队中 → 生成中 → 合成中 → 质检中 → 待审核`
- 异常：`失败/部分失败 → 自动重试 → 人工处理或降级 → 返回对应步骤`
- 审核：`待审核 → 审核中 → 通过 / 驳回 / 待人工复核`
- 素材：`待入库 → 已入库 → 待投放 → 投放中 → 已下线 → 可复用/归档`

重大修改要创建新版本并保留历史，不能静默覆盖。

## 工程与文件纪律

- `product-map/` 负责产品全景大图，遵循其中更具体的 `AGENTS.md`。
- `demo/` 负责交互原型，遵循其中更具体的 `AGENTS.md`。
- `docs/` 保存产品定义、流程、状态和决策记录。
- 首版优先使用 HTML、CSS、JavaScript 和集中管理的 Mock 数据。
- 未经用户确认，不引入大型框架、收费组件、真实 API 或必须联网的资源。
- 业务数据、状态配置和渲染逻辑分离；节点与任务使用稳定 ID。
- 不把所有内容堆进一个超长 HTML 文件。
- 完成页面修改后必须在浏览器中实际检查，不能只读代码推测正确。

## 共同验收标准

- 业务链路从运营意图一直闭环到数据回流；
- 核心节点均说明输入、处理、输出、状态、异常和上下游；
- 图片与视频生产链路清楚分开；
- 生产队列、审核队列、失败返回和人工介入路径明确；
- 素材库、知识库和资产库边界清楚；
- 页面在 1440px 与 1920px 宽度下可用；
- 不联网时仍能展示核心内容；
- 浏览器控制台无错误；
- README 与产品文档随关键行为变化同步更新。

## 优先级

发生取舍时按以下顺序处理：

1. 业务链路正确；
2. 状态、责任和异常清楚；
3. 可追溯、可解释；
4. 用户容易理解；
5. 可扩展为 Demo；
6. 视觉精致；
7. 动效和装饰。
